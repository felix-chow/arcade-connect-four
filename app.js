// Initialize board to empty array
const board = [];

// Loop through all 7 columns of the board
for (let i = 0; i <= 7; i++) {
  for (let j = 0; j <= 6; i++) {
    
  }
  // board.push([])
}

const gameState = {
    board: board, // from above
    players: []
  }